interface:
  display_name: "Setup GH"
  short_description: "Install and authenticate GitHub CLI safely."
  default_prompt: "Use $setup-gh to install GitHub CLI on this machine and verify that login succeeds."

policy:
  allow_implicit_invocation: true
