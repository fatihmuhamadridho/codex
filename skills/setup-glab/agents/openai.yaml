interface:
  display_name: "Setup GLab"
  short_description: "Install and authenticate GitLab CLI safely."
  default_prompt: "Use $setup-glab to install GitLab CLI on this machine and verify that login succeeds for the intended GitLab host."

policy:
  allow_implicit_invocation: true
