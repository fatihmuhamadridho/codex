interface:
  display_name: "Repo Conventions"
  short_description: "Apply repo-wide rules and completion checks."
  default_prompt: "Use $repo-conventions to verify this change follows the repo's existing patterns and is actually done."

policy:
  allow_implicit_invocation: true
